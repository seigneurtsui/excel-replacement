{
  "name": "excel-replacer-hono",
  "scripts": {
    "dev": "wrangler pages dev public",
    "build": "wrangler pages functions build --outdir public --plugin ./src/index.ts",
    "deploy": "npm run build && wrangler pages deploy public"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "xlsx": "^0.18.5",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.0.0"
  }
}
